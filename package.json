{
  "name": "frontful-utils",
  "version": "3.0.3",
  "private": true,
  "description": "Utilities for Javascript applications",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/frontful/frontful-utils.git"
  },
  "main": "./utils/index.js",
  "scripts": {
    "build": "yarn test && babel-preset-frontful build",
    "deploy": "yarn test && babel-preset-frontful deploy",
    "test": "yarn test:eslint",
    "test:eslint": "node ./node_modules/eslint/bin/eslint.js --max-warnings 0 ."
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "dependencies": {
    "cookie": "^0.3.1"
  },
  "devDependencies": {
    "babel-preset-frontful": "3.x",
    "eslint-config-frontful": "3.x"
  }
}
